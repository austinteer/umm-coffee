import {
  timestamp
} from '../../../helpers/timestamp';
import { module, test } from 'qunit';

module('TimestampHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = timestamp(42);
  assert.ok(result);
});
